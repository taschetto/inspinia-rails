<% content_for :title, "Lançamento #{@time_entry.id}" %>
<% breadcrumb :time_entry, @time_entry %>

<div class="ibox">
  <div class="ibox-content">
    <div class="row">
      <div class="col-lg-12">
        <div class="m-b-md">
          <%= link_to "Editar", edit_time_entry_path(@time_entry), class: "btn btn-white btn-xs pull-right" %>
          <h1><%= @time_entry.task.name %></h1>
          <h3><%= @time_entry.task.description %></h3>
        </div>

        <dl class="dl-horizontal">

        </dl>
      </div>
    </div>
    <div class="row">
      <div class="col-lg-6">
        <dl class="dl-horizontal" >
          <dt>Iniciado em:</dt> <dd><%= @time_entry.starts_at %></dd>
          <dt>Finalizado em:</dt> <dd><%= @time_entry.ends_at %></dd>
          <dt>Memo:</dt> <dd><%= @time_entry.memo %></dd>
        </dl>
      </div>    
      <div class="col-lg-6">
        <dl class="dl-horizontal" >
          <dt>Atualizado em:</dt> <dd><%= @time_entry.updated_at %></dd>
          <dt>Criado em:</dt> <dd><%= @time_entry.created_at %></dd>
        </dl>
      </div>
    </div>
  </div>
</div>